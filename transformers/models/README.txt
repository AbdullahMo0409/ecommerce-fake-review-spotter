# Transformer Model Files

This folder contains the lightweight configuration and tokenizer files used by the DistilBERT-based fake review detection model.

The trained model checkpoint file is not included in this repository because of its large size.

Excluded file:
- `model.safetensors`

The model can be retrained using the provided transformer source code and training files.